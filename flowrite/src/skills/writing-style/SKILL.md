---
name: writing-style
description: Prose and code style rules for ZIO documentation (reference pages, how-to guides, tutorials). Load whenever writing or editing docs to keep prose consistent, clear, and professional.
---

# ZIO Documentation Writing Style

<!-- TEMP: flue can't package nested skill files, so `references/rules.md` is unreadable at runtime (read_skill_resource 404s) — https://github.com/withastro/flue/discussions/100. The rules are injected into the task prompt instead. REVERT to "Read that file now" once flue supports nested skills. -->

The complete numbered rule list is provided verbatim in your task input (single source of truth: `references/rules.md`, also consumed programmatically by the mechanical style checker). Apply every rule to each documentation page you write, edit, or review. When citing a violation, reference the rule by its number (e.g. "writing-style #8").
