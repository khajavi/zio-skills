# Notes on the optics module

This file deliberately has no frontmatter block. It is a working note, not a Docusaurus doc page,
and a backfiller has no business inventing an `id` and a `title` for it.

description: this line is not frontmatter, and neither is the next one
keywords: still not frontmatter

- `Lens` for a field that is always present
- `Prism` for a case that may not match
- `Optional` for the composition of the two
