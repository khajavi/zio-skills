#!/bin/bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(dirname "$SCRIPT_DIR")"

if [ -f "$REPO_ROOT/skills/zio-http-scaffold/SKILL.md" ]; then
    cat "$REPO_ROOT/skills/zio-http-scaffold/SKILL.md"
fi
